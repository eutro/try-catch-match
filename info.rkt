#lang info

(define collection "try-catch-match")
(define deps '("base"))
(define pkg-desc "A try-catch-finally macro that binds with match.")
(define version "0.1")

(define build-deps '("base" "scribble-lib" "racket-doc"))
(define scribblings '(("scribblings/try-catch-match.scrbl" ())))
